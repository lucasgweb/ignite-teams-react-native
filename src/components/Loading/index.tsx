import { Container, LoadingIndicator } from './styles'
import React from 'react'

export const Loading = () => {
    return (
        <Container>
            <LoadingIndicator />
        </Container>
    )
}